interface IRoutes {
  categoriesList: '/categories';
  currentCategory: '/categories/category/';
}

export const routes: IRoutes = {
  categoriesList: '/categories',
  currentCategory: '/categories/category/',
};
