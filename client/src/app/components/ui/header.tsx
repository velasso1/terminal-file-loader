import { FC } from 'react';

const Header: FC = () => {
  return (
    <header className="header">
      <div className="header__title">Панель управления контентом терминала</div>
    </header>
  );
};

export default Header;
